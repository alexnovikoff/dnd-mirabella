# Секрет для подписи JWT-сессий Auth.js. Сгенерировать: openssl rand -base64 32
AUTH_SECRET=

# Пароль, которым засевается пятёрка учёток (только для локальной разработки).
# По умолчанию «мирабелла».
SEED_PASSWORD=
