-- Размер плитки на доске связей, в процентах от обычной. Хранится рядом
-- с координатами: это та же общая раскладка кампании. У всех узлов — 100.
--
-- Миграция только добавляет колонку со значением по умолчанию — на Neon её
-- накатывают ДО мёржа: прежняя сборка колонку просто не выбирает.
ALTER TABLE "board_positions" ADD COLUMN "size" integer DEFAULT 100 NOT NULL;
