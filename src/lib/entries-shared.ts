/* Общее про записи. Отдельный файл, чтобы шит быстрой записи мог взять
 * константу, не таща за собой lib/queries с базой и argon2. */

/** Лут — это момент с этим тегом, а не отдельный тип записи: он живёт везде,
 *  где моменты (сессия, персонаж, доска), и отличается только фильтром ленты. */
export const LOOT_TAG = '#лут';
