/* Общее про выбор сессии. Отдельный файл, чтобы константу можно было
 * импортировать и в клиентских компонентах, не таща за собой lib/queries —
 * а с ним базу, сид и argon2, которым в браузерном бандле места нет. */

/** Значение селекта, означающее «запись вне сессий». Пустая строка не годится:
 *  она значит «не выбирали» — там подставляется активная сессия. */
export const NO_SESSION = 'none';
